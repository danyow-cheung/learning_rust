fn main(){
    let lazy_caterer:[u32;6] = [1,2,3,4,5,11];
    let taxonomy = ["Animalia", "Arthropoda", "Insecta"];

    assert_eq!(lazy_caterer[3],4);
    assert_eq!(taxonomy.len(),3);
    // 排序
    let mutchaos=[3,5,4,1,2]; 
    chaos.sort();
    assert_eq!(chaos, [1, 2, 3, 4, 5]);
    
}
